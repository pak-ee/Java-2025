Практика №1

После выполнения `a1 = a2;` ссылка `a1` начинает указывать на тот же массив `{6,7,8,9,10}`, что и `a2`. Исходный массив `{1,2,3,4,5}` становится недостижимым (его может собрать GC). Любые изменения через `a1` или `a2` будут затрагивать один и тот же массив.
