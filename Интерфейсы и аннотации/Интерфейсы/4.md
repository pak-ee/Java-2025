# Практика #4

```java
private interface Interface { ... }
```

Если это интерфейс верхнего уровня (top-level), так объявить нельзя — ошибка компиляции.
`private` возможен только у вложенного интерфейса — тогда он доступен только внутри внешнего класса/интерфейса.

